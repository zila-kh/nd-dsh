node_modules/
out/
dist/
.release/
coverage/
test-results/
# Real-user production E2E evidence: one directory per run, never committed.
e2e-results/
.smoke/
target/
.DS_Store
.env
.env.*
!.env.example
!.env.e2e.example
*.log
.sessions/
.nd-dsh/
artifacts/
.pnpm-store/

# Keep project skills tracked while ignoring local DSH runtime state.
.dsh/*
!.dsh/skills/
!.dsh/skills/**

# Pinned upstream infrastructure populated by git submodule/bootstrap.
/vendor/deepseek-harness/
/vendor/openpencil/
!/vendor/README.md
!/vendor/deepseek-harness.json
!/vendor/openpencil.json
!/vendor/openpencil.LICENSE

# Built ND Pencil runtime staged for packaging; never commit platform binaries.
/resources/nd-pencil/bin/

*.tsbuildinfo

# ZCode session artifacts
.zcode/

# Local scratch space for ad-hoc dev runs.
scratch/

# Todo example local runtime state (npm cache, logs, JSON data store)
examples/todo/cache/
examples/todo/data/
beta-qa-report-**
# Local performance evidence; reviewed baselines are copied intentionally.
benchmark-results/
