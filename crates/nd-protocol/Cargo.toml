[package]
name = "nd-protocol"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"
license = "MIT"
publish = false

[dependencies]
anyhow = "1"
rmp-serde = "1.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
